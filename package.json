{
    "name": "livesomali-deploy",
    "version": "1.0.0",
    "engines": {
        "node": ">=18.0.0"
    },
    "scripts": {
        "build": "cd client && npm install && npm run build",
        "postinstall": "cd server && npm install",
        "start": "node server/server.js"
    }
}